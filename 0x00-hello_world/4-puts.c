#include <stdio.h>
/**
 * main- function is to use puts to print string of characters.
 * Return 0 (Success).
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
		/**
		 *  puts function functions in a similar way to printf
		 */
	return (0);
}

#include <stdio.h>
/**
 * main- function is for our code to use prrintf to print a string of characters.
 * Return 0 (Success)
 */
int main (void)
{       
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

